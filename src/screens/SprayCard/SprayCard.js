import React, {useState} from "react";
import Header from "../../components/Header";
import Background from "../../components/Background";

export default function SprayCard({navigation}) {

    return (
        <Background>
            <Header>Spray Card</Header>
        </Background>
    )
}